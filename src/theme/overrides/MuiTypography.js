export const MuiTypography = {
    styleOverrides: {
        root: ({ theme }) => ({
            color: theme.palette.text.primary,
        }),

        h1: ({ theme }) => ({
            fontWeight: 700,
            color: theme.palette.text.primary,
        }),

        h2: ({ theme }) => ({
            fontWeight: 700,
            color: theme.palette.text.primary,
        }),

        h3: ({ theme }) => ({
            fontWeight: 600,
            color: theme.palette.text.primary,
        }),

        h4: ({ theme }) => ({
            fontWeight: 600,
            color: theme.palette.text.primary,
        }),

        h5: ({ theme }) => ({
            fontWeight: 600,
            color: theme.palette.text.primary,
        }),

        h6: ({ theme }) => ({
            fontWeight: 600,
            color: theme.palette.text.primary,
        }),

        subtitle1: ({ theme }) => ({
            color: theme.palette.text.secondary,
            fontWeight: 500,
        }),

        subtitle2: ({ theme }) => ({
            color: theme.palette.text.secondary,
            fontWeight: 500,
        }),

        body1: ({ theme }) => ({
            color: theme.palette.text.primary,
        }),

        body2: ({ theme }) => ({
            color: theme.palette.text.secondary,
        }),

        caption: ({ theme }) => ({
            color: theme.palette.text.secondary,
        }),
    },
};
